const Navbar = () => {
  return (
    <article className="Navbar">
      <div>
        <a href="#">Home</a>
        <a href="#">Projects</a>
        <a href="#">Contacts</a>
      </div>
    </article>
  );
};
export default Navbar;
